import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.realtor.app',
  appName: 'realtors',
  webDir: 'dist/realtors/browser'
};

export default config;
